# Gameplay

v2.0.0 makes the public demo playable.

The game runs as a browser canvas:

```text
moving packets
boundary gate
touch controls
keyboard controls
trust / risk / integrity metrics
release blocked / victory states
```

The page does not require a video file. It also does not require a loaded WASM artifact for the visible gameplay loop.
