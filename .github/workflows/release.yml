name: Release

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  release:
    name: release
    runs-on: ubuntu-latest
    steps:
      - run: echo "Release workflow is manual-only until final signed v7.3.0."
