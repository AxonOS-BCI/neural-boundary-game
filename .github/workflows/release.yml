name: Release

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  release:
    name: Foundation Release / manual final gate
    runs-on: ubuntu-latest
    steps:
      - run: echo "Manual-only. Use final signed v7.3.0 after audit closure."
