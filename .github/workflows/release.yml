name: Release

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Release workflow is manual-only. Use after v7.3.0 final audit."
