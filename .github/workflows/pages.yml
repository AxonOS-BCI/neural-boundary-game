name: GitHub Pages

on:
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Pages deployment is manual-only until v7.3.0 CI is stable."

  deploy:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - run: echo "Manual Pages deploy placeholder."
