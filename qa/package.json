{
  "name": "neural-boundary-game-qa",
  "version": "7.3.0",
  "private": true,
  "scripts": {
    "test": "playwright test",
    "test:ci": "playwright test --reporter=junit"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0"
  }
}
