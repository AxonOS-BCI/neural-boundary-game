[build]
target = "web/index.html"
dist = "dist"
public_url = "./"

[watch]
watch = ["crates/neural-boundary-core", "crates/neural-boundary-web", "web"]

[serve]
addresses = ["127.0.0.1"]
port = 8080
open = false
