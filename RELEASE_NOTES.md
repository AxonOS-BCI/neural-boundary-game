# Neural Boundary Game v1.7.0

Elite AxonOS Standard Foundation Grande Style.

## Highlights

- release and tag badges added to README;
- GitHub Release workflow added for signed tags;
- real Pages deployment workflow restored with `actions/deploy-pages`;
- new core review helpers: `release_ready`, `boundary_status`, `review_summary`, `apply_script`;
- CLI now prints release-readiness and gate summary;
- WASM adapter exposes deterministic review flags internally;
- MIT-only license surface for cleaner GitHub sidebar detection;
- keeps 12 CI jobs;
- keeps `#![forbid(unsafe_code)]`;
- keeps `no_std` deterministic core;
- keeps no-`web-sys`, no-`wasm-bindgen`, no-`#[no_mangle]`.

## Claim hygiene

This is an educational technical demo. It does not process real signal data or control stimulation hardware. It is not a medical device, clinical system, safety-certified system, security audit, or regulatory review.
